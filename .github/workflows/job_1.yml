name: job_1

on:
  schedule:
    - cron:  '0 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          ref: 'main'
      - uses: actions/setup-python@v4
      - run: pip install --upgrade pip
      - run: pip install poetry
      - run: poetry install -C ./job_1 
      - run: python -m job_1.job_1.main
